# Alerts Panel

Serve on a vituslhost with your alertmanager reachable under `/alert-manager` for a tabular panel that shows all your alerts.

alerts are colored by `severity` label, which can be one of "info", "warning", "error", "high", "disaster".